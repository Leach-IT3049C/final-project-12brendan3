let levels = [];

function readMaps() {
  levels.push(`Level One`);
  levels.push(`Level Two`);
  levels.push(`Level Three`);
  levels.push(`Level Four`);
  levels.push(`Level Five`);
  levels.push(`Level Six`);
  levels.push(`Level Seven`);
  levels.push(`Level Eight`);
}

function getLevels() { // Gets levels from maps
  return levels;
}